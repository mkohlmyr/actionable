name: plan

on: [push]

jobs:
  plan:
    runs-on: ubuntu-latest
    name: Create a terraform plan
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Test TF
        id: terraform
        uses: ./
        with:
          script: plan.sh
      - name: Hello World
        run: echo "hello world!"