name: plan

on: [push]

jobs:
  plan:
    runs-on: ubuntu-latest
    name: Create a terraform plan
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Hello World
        run: echo "hello world!"